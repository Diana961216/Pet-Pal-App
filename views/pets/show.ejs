<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/pets.css" />
    <link rel="stylesheet" href="/stylesheets/global.css" />
    <title><%= pet.name %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <div class="pet-show-card">
      <h1><%= pet.name %></h1>
      <p><strong>Breed:</strong> <%= pet.breed %></p>
      <p><strong>Age:</strong> <%= pet.age %></p>
      <p><strong>Gender:</strong> <%= pet.gender %></p>
      <p><strong>Owner:</strong> <%= pet.owner.name || pet.owner.email %></p>

      <div class="pet-show-buttons">
        <a href="/pets/<%= pet._id %>/edit" class="btn secondary-btn">Edit</a>
        <form action="/pets/<%= pet._id %>?_method=DELETE" method="POST">
          <button
            type="submit"
            class="btn secondary-btn"
            onclick="return confirm('Are you sure you want to delete this pet?')"
          >
            Delete
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
